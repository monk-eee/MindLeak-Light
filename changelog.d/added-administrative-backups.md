- Add an unreleased CLI for encrypted restic/PostgreSQL backups, coherent capture,
  immutable source assets, owned restore verification, explicit retention, status,
  and opt-in scheduler/Azure adapters. Existing MCP startup remains compatible;
  read-only verification skips schema initialization. Production support remains
  gated on native-platform and approved remote-recovery acceptance.
