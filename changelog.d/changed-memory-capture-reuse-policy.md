- Update the companion memory policy to v1.2.1 with explicit post-verification
  capture checkpoints, hierarchy-first startup, optional mode-aware retrieval
  with bounded query refinement, progressive evidence inspection,
  duplicate-aware writes and recorded application outcomes. Repeated runs do not
  imply independent confirmation. Keep the canonical policy, onboarding and server reminder
  synchronized without changing scope, tool approvals or lifecycle semantics.
