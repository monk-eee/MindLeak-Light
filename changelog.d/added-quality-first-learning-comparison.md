- Add an explicit Lab 3 quality profile comparing frozen original and explicitly
  reviewed knowledge with independent notebook and fresh-agent controls on new
  matched cases. Keep existing profiles and historical results unchanged.
- Retain measured exceptions, original validation history and immutable original
  knowledge. Require fresh prospective validation and explicit acceptance after
  revision; failed capture or incomplete review cannot produce a complete study.
- Separate case proposals, exact-reference acceptance and principle proposal into
  bounded decisions. Track per-case no-change receipts with idempotent retries;
  acknowledged outcomes, not a generic completion answer, decide phase completion.
- Score required behavior, boundary handling, regression safety and source-backed
  decisions separately. Show correct rejection and unmeasured evidence without
  conflating them with application, record counts, speed or token savings.
- Keep the added quality audit outside the agent's visible test loop; grade its
  frozen candidate in a separate sandbox and retain both public and audit receipts.
- Add exact-source, failed-persistence and fixture-bound receipt regressions. The
  held model-extraction draft remains excluded; semantic fidelity and real-agent
  quality gains are not inferred from structural checks or deterministic fixtures.
