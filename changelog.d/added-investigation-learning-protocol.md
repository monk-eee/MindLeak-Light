- Add an explicit Lab 3 v4 mechanism profile that retains checked discoveries from unfinished tasks, forms principles from separate investigations, and records predictions before reserved validation.
- Compare later decisions against independently authored searchable notes and fresh agents, with frozen knowledge, separate direct-delivery diagnostics, and preparation/review costs retained.
- Preserve measured counterexamples through explicit post-comparison challenge and revision; revised candidates require fresh validation cases. Keep existing v2/v3 labs, recordings and MCP defaults unchanged.
- Build on PR #41's live learning stage, growing graph, motion, autoplay and profile controls without changing its styling, multi-principle workflows or default Learning profile.
- Retain failed reserved executions and complete scheduled denominators, actual runtime/model identity and study usage totals. Keep initial real-model results separate from subsequent reporting fixes.
- Bring learned conclusions, conditions, linked supporting chains and counterevidence forward on the Knowledge page, with selectable principles and formation replay. Add a Lab-3-only case board, prospective prediction reveals and recorded chapter controls within the existing live stage.
- Give Knowledge its own animated assembly bench: source intake, chain collation, principle catalogue and selectable evidence lineage. Preserve PR #41's experiment stage, replay controls, pause and reduced motion; paired storage receipts retain their logical knowledge identity for actual arrival pulses.
- Name the synthesis view Knowledge Factory, with factory and workstation icons, striped rails and a conveyor that moves only with recorded activity.
- Align the Knowledge review cockpit with Labs 1 and 2: bold outlines, colored icon counters, blue primary controls and the same crisp record shadows, while retaining its factory layout.
- Reuse Lab 2's animated character nodes throughout Lab 3, with task start/finish events replacing stale preparation states. Brighten the Knowledge machine with colored station badges, layered source slips and highlights tied to recorded arrivals.
- Lead Knowledge Control with a human review queue, readable principle playbook and source-evidence workspace. Confirm and export browser-local decisions bound to exact revisions and evidence, without changing MCP acceptance or sealed experiment results; live runs and historical playback remain locked for sign-off.
- Add explicit trusted-private-LAN HTTP access for the native lab, retaining loopback defaults, exact Host/Origin guards and same-origin navigation/events. This lab-only mode has no authentication or TLS and leaves PostgreSQL and MCP exposure unchanged.
- Make new main Lab 3 runs knowledge-first: MindLeak and notebook agents must look up prior experience, assess it against inspected current evidence, then apply/adapt/reject and verify. Preserve optional-adoption v3 as a separate profile and all historical results. Show lookup, delivery, assessment and application separately; a lookup alone never earns reuse credit.
- Make Lab 1's memory team publish verified component handoffs and read every current dependency source through MCP before editing. Separate sharing completion from code checks, retain inherited findings in continued reports, and leave no-memory controls and historical optional-sharing results unchanged.
- Fix Lab 1 network counters to track received handoffs and its visible team, animate pending tool work, clear finished activity, and place the hub/connectors below the rendered cards. Replace irrelevant formation/reuse displays with build-sharing status.
- Make memory-enabled startup knowledge-first across the companion policy and lab agents: scoped search, original-source inspection and current-evidence assessment before editing or downstream investigation. Preserve explicit independent-discovery and no-memory controls, and record real lookup misses/errors without awarding reuse.
- Show Lab 1 startup search/assessment counts separately from handoffs, count Lab 2 live use only against an earlier source-backed application receipt, and keep unmeasured reuse values unknown.
- Expose exact pending guide acceptance IDs and the next action after proposal; keep failed and unaccepted knowledge stages visible without rerunning or relabelling historical results.
- Pin Lab 1 dependency handoffs to module hashes and require consumers to read those current files before editing or publishing. Apply/adapt assessments must cite the dependency implementation, not an unrelated pending stub; report these checks separately from memory delivery.
- Enforce the documented active/expired status badges in Lab 1's immutable view checks. Keep the 18 named checks and preserve older recordings under their original fixture hashes.
- Bound Lab 2 guide synthesis to one source-backed dossier followed by a read-free decision phase: propose/revise, explicitly accept, or record no new learning. Preserve conditions and counterevidence, require current review state, fail oversized evidence, and retain all source preparation and failed-attempt costs without increasing retries.
- Fix Lab 2 guide application after case-chain-only retrieval: require a real current principle before assessment, provide explicit inspection pointers and typed ID/revision references, preserve pre-assessment eligibility, and reject wrong-kind or stale application targets without changing retrieval scores or historical results.
