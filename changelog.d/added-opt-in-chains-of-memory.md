- Add the full opt-in observations-to-chains-to-principles workflow within the
  existing MCP tools: propose, validate, challenge, revise, retire and inspect
  immutable history without changing default fact recall.
- Preserve source episodes, complete fragments, keyed receipts, optional scope,
  counterevidence, and immutable revisions in the existing three tables. Reject
  stale revisions, normalized duplicate claims, and chain-derived evidence.
- Add separately enabled model-assisted candidate formation with strict output,
  exact citations, evidence gaps and retained model/prompt/source provenance;
  previews never store or accept knowledge and provider failures never fall back.
- Add principles-first keyword/pgvector/hybrid retrieval with original branch
  scores, configured relevance, direct and inherited counterevidence, pinned
  validated chain support, deduplicated source lineage and independent observations.
- Expose stale dependency review, paged dependents and bounded JSON/Markdown
  exports. Revisions preserve known counterexamples. No automatic acceptance,
  confidence promotion, background worker, source deletion or client UI is added.
- Make backup canaries schema-aware and verify restored knowledge separately from
  observations; include knowledge vectors and lineage in the encrypted roundtrip.
- Add an explicit six-subject knowledge benchmark and CI evidence, retain real
  model failures/results, and simplify onboarding and architecture documentation.
- Restrict native release assets to binary packages and checksums; CI and
  benchmark reports stay in their own workflow artifacts.
